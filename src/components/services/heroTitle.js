/* Single source of truth for the service hero title typography.
   Every service Experience (Salesforce, AI & ML, Snowflake, …) uses this so
   all hero titles share the exact same size, responsive scaling, line-height,
   and tracking — no service appears larger or smaller than another. */
export const HERO_TITLE_CLASS =
  'font-display font-bold leading-[0.9] tracking-tight text-[15vw] md:text-[10vw] lg:text-[8.5rem]';
